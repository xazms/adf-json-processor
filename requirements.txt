# Core dependencies
requests>=2.31        # ✅ Latest stable version (fixes security issues)
pyspark>=3.4          # ✅ Updated for better performance and security
sqlparse>=0.4.4       # ✅ Updated for security fixes
pygments>=2.17        # ✅ Updated to fix syntax highlighting issues

# Development dependencies
pytest>=7.4.0         # ✅ Updated to latest stable version
pytest-cov>=4.0.0     # ✅ Ensures compatibility with newer pytest
flake8>=5.0.4         # ✅ Linting for code style enforcement
black>=24.3.0         # ✅ Fixes Regular Expression Denial of Service (ReDoS)
pre-commit>=3.5.0     # ✅ Keeps formatting stable
setuptools>=70.0      # ✅ Fixes Command Injection vulnerability